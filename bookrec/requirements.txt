# Core (the framework runs on these)
numpy<2.0            # required: scikit-surprise compiled against NumPy 1.x
pandas
scikit-learn
streamlit

# Collaborative filtering (from class)
scikit-surprise      # if pip can't build a wheel: conda install -c conda-forge scikit-surprise

# LLM re-ranking layer (assignment)
google-genai            # Week-4 SDK: structured output (response_schema) — preferred
google-generativeai     # legacy text-only fallback if google-genai is unavailable
pydantic                # typed response schemas for structured output
python-dotenv

# Optional "go beyond class" upgrades (in-memory vectors — no ChromaDB)
# sentence-transformers   # semantic book embeddings (content_model backend="embeddings")
# faiss-cpu               # only if catalog grows well beyond ~10k books
